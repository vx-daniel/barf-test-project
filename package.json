{
  "name": "barf-test-project",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "reset": "git reset --hard HEAD && git clean -fd"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}