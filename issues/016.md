---
id=016
title=Fix verification failures: 009-1
state=NEW
parent=009-1
children=
split_count=0
force_split=false
needs_interview=true
verify_count=0
is_verify_fix=true
---

## Context
Issue 009-1 was marked COMPLETED but failed automated verification.

## Failures

### build
```
error: Script not found "build"
```

### check
```
error: Script not found "check"
```

## Acceptance Criteria
- [ ] `bun run build` passes
- [ ] `bun run check` passes (format + lint)
- [ ] `bun test` passes

## Interview Questions

1. Should the fix involve adding the missing 'build' and 'check' scripts to package.json, or fixing the implementation in 009-1 to work with existing scripts?
   - Add missing scripts to package.json
   - Fix implementation in 009-1
   - Both scripts need investigation first
2. What should the 'build' script do? (e.g., compile TypeScript, bundle, run tsc, etc.)
   - Compile TypeScript with tsc
   - Run all build-related tasks
   - Other (specify in implementation)
3. What should the 'check' script do exactly? (e.g., only format check without changes, or both format and lint, or specific tool setup?)
