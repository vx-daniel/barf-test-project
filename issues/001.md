---
id=001
title=Basic arithmetic (add/subtract/multiply/divide)
state=COMPLETED
parent=
children=
split_count=0
force_split=false
---

## Description
Implement the four basic arithmetic operations as pure functions.

## Acceptance Criteria
- [x] `add(a, b)` returns the sum
- [x] `subtract(a, b)` returns the difference
- [x] `multiply(a, b)` returns the product
- [x] `divide(a, b)` returns the quotient, throws on division by zero
- [x] All functions have tests
