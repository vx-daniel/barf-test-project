---
id=024
title=Fix verification failures: 005
state=COMPLETED
parent=005
children=
split_count=0
force_split=false
verify_count=0
is_verify_fix=true
---

## Context
Issue 005 was marked COMPLETED but failed automated verification.

## Failures

### check
```
Checked 12 files in 3ms. No fixes applied.
Found 1 error.

$ bun run format && bun run lint
$ bun x biome format src/ tests/
tests/rounding.test.ts format ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Formatter would have printed the following content:
  
     5  5 │     it('rounds to 0 decimal places', () => expect(roundTo(1.5, 0)).toBe(2))
     6  6 │     it('rounds to 2 decimal places', () => expect(roundTo(1.2345, 2)).toBe(1.23))
     7    │ - ··it('rounds·up·when·next·digit·>=·5',·()·=>·expect(roundTo(1.2355,·2)).toBe(1.24))
        7 │ + ··it('rounds·up·when·next·digit·>=·5',·()·=>
        8 │ + ····expect(roundTo(1.2355,·2)).toBe(1.24))
     8  9 │     it('handles negative numbers', () => expect(roundTo(-1.2345, 2)).toBe(-1.23))
     9 10 │     it('handles zero', () => expect(roundTo(0, 2)).toBe(0))
    10    │ - ··it('handles·floating-point·edge·case·1.005',·()·=>·expect(roundTo(1.005,·2)).toBe(1.01))
    11    │ - ··it('rounds·to·3·decimal·places',·()·=>·expect(roundTo(3.14159,·3)).toBe(3.142))
       11 │ + ··it('handles·floating-point·edge·case·1.005',·()·=>
       12 │ + ····expect(roundTo(1.005,·2)).toBe(1.01))
       13 │ + ··it('rounds·to·3·decimal·places',·()·=>
       14 │ + ····expect(roundTo(3.14159,·3)).toBe(3.142))
    12 15 │     it('handles integer input', () => expect(roundTo(5, 2)).toBe(5))
    13 16 │   })
    ····· │ 
    17 20 │     it('ceils to integer when decimals is 0', () => expect(ceil(1.1, 0)).toBe(2))
    18 21 │     it('ceils to 2 decimal places', () => expect(ceil(1.231, 2)).toBe(1.24))
    19    │ - ··it('returns·same·value·if·already·exact',·()·=>·expect(ceil(1.5,·1)).toBe(1.5))
       22 │ + ··it('returns·same·value·if·already·exact',·()·=>
       23 │ + ····expect(ceil(1.5,·1)).toBe(1.5))
    20 24 │     it('handles negative numbers', () => expect(ceil(-1.9)).toBe(-1))
    21    │ - ··it('handles·negative·with·decimals',·()·=>·expect(ceil(-1.231,·2)).toBe(-1.23))
       25 │ + ··it('handles·negative·with·decimals',·()·=>
       26 │ + ····expect(ceil(-1.231,·2)).toBe(-1.23))
    22 27 │     it('handles zero', () => expect(ceil(0)).toBe(0))
    23 28 │     it('ceils to 1 decimal place', () => expect(ceil(1.01, 1)).toBe(1.1))
    ····· │ 
    25 30 │   
    26 31 │   describe('floor', () => {
    27    │ - ··it('floors·to·integer·when·decimals·omitted',·()·=>·expect(floor(1.9)).toBe(1))
    28    │ - ··it('floors·to·integer·when·decimals·is·0',·()·=>·expect(floor(1.9,·0)).toBe(1))
       32 │ + ··it('floors·to·integer·when·decimals·omitted',·()·=>
       33 │ + ····expect(floor(1.9)).toBe(1))
       34 │ + ··it('floors·to·integer·when·decimals·is·0',·()·=>
       35 │ + ····expect(floor(1.9,·0)).toBe(1))
    29 36 │     it('floors to 2 decimal places', () => expect(floor(1.239, 2)).toBe(1.23))
    30    │ - ··it('returns·same·value·if·already·exact',·()·=>·expect(floor(1.5,·1)).toBe(1.5))
       37 │ + ··it('returns·same·value·if·already·exact',·()·=>
       38 │ + ····expect(floor(1.5,·1)).toBe(1.5))
    31 39 │     it('handles negative numbers', () => expect(floor(-1.1)).toBe(-2))
    32    │ - ··it('handles·negative·with·decimals',·()·=>·expect(floor(-1.231,·2)).toBe(-1.24))
       40 │ + ··it('handles·negative·with·decimals',·()·=>
       41 │ + ····expect(floor(-1.231,·2)).toBe(-1.24))
    33 42 │     it('handles zero', () => expect(floor(0)).toBe(0))
    34 43 │     it('floors to 1 decimal place', () => expect(floor(1.99, 1)).toBe(1.9))
    35 44 │   })
    36 45 │   
    37    │ - describe('roundHalfEven·(banker\'s·rounding)',·()·=>·{
       46 │ + describe("roundHalfEven·(banker's·rounding)",·()·=>·{
    38 47 │     it('rounds 0.5 to 0 (even)', () => expect(roundHalfEven(0.5)).toBe(0))
    39 48 │     it('rounds 1.5 to 2 (even)', () => expect(roundHalfEven(1.5)).toBe(2))
  

format ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  × Some errors were emitted while running checks.
  

error: script "format" exited with code 1
error: script "check" exited with code 1
```

## Acceptance Criteria
- [x] `bun run build` passes
- [x] `bun run check` passes (format + lint)
- [x] `bun test` passes
