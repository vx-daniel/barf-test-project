---
id=012
title=Range and clamp
state=NEW
parent=
children=
split_count=0
force_split=false
---

## Description
Implement range generation and value clamping utilities.

## Acceptance Criteria
- [ ] `range(start, end, step?)` generates an array of numbers
- [ ] `clamp(value, min, max)` constrains a value within bounds
- [ ] `inRange(value, min, max)` returns true if value is within bounds
- [ ] Tests cover ascending/descending ranges, step sizes, and boundary conditions
