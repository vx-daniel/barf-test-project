# Plan 020 — Fix verification failures: 004

## Problem

Issue 004 was marked COMPLETED but failed `bun run check` (biome format).
Two files have long lines that biome wants broken into multi-line form:

1. **`src/index.ts:128`** — ternary in `toScientific` too long for one line
2. **`tests/formatting.test.ts:50`** — `.toThrow(...)` call too long for one line

## Design Decisions

- **Use biome's auto-formatter** (`biome format --write`) rather than manual edits.
  This guarantees the output matches exactly what biome expects and prevents
  introducing new issues.
- No logic changes; only whitespace / line-break changes.

## Acceptance Criteria → Test Mapping

| Criterion | Verification command |
|---|---|
| `bun run build` passes | `bun run build` (tsc --noEmit) |
| `bun run check` passes | `bun run check` (biome format + lint) |
| `bun test` passes | `bun test` |

No new tests needed — this is a formatting-only fix. Existing tests must continue to pass.

## Steps

### 1. Auto-format with biome (the fix)
```bash
bun x biome format --write src/index.ts tests/formatting.test.ts
```

### 2. Verify: `bun run check`
```bash
bun run check
```
Expected: exit 0, no errors.

### 3. Verify: `bun run build`
```bash
bun run build
```
Expected: exit 0.

### 4. Verify: `bun test`
```bash
bun test
```
Expected: all tests pass.
